{
  "name": "ttrpg-club",
  "version": "1.0.0",
  "description": "TTRPG Club website with dynamic markdown content loading",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "dev": "node build.mjs --watch",
    "serve": "py -3 -m http.server 8000"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "esbuild": "^0.25.11",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "marked": "^11.0.0"
  }
}
